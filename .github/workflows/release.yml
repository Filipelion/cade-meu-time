name: Build Release ZIP

on:
  workflow_dispatch:
  pull_request:
    types: [labeled]

jobs:
  test:
    uses: ./.github/workflows/test.yml

  build:
    needs: test
    if: github.event_name == 'workflow_dispatch' || github.event.label.name == 'release'
    runs-on: ubuntu-slim

    steps:
      - uses: actions/checkout@v4

      - name: Read version from manifest
        id: version
        run: echo "version=$(jq -r '.version' src/manifest.json | tr '.' '_')" >> $GITHUB_OUTPUT

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: MaiorDoNordeste_SportRecife-v${{ steps.version.outputs.version }}
          path: src/
